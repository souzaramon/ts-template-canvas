{
  "name": "ts-template-canvas",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "vite",
    "test": "vitest --passWithNoTests",
    "coverage": "vitest run --coverage",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.9.9",
    "vitest": "^0.13.0"
  }
}
